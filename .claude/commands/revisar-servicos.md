---
description: Revisão minuciosa dos textos e da substância dos descritivos de serviço do site
argument-hint: "[página ou área específica — opcional]"
---

Invoque a skill `revisar-servicos` com `$ARGUMENTS` e siga o que ela diz.

<!--
POR QUE ESTE ARQUIVO É UMA LINHA.

Até 04/09/2026 ele carregava uma CÓPIA inteira do procedimento — e as duas
cópias divergiram, como sempre acontece. A skill já tinha evoluído para um
critério de parada auditável ("a rodada encerra quando a lista de defeitos
verificáveis zera, e não por autoavaliação; não se atribua nota"), enquanto este
comando ainda mandava o oposto:

    "Só se dê por satisfeito quando a nota for maior que 95."

Nota que quem executa dá a si mesmo. Quem produz o trabalho e dá a nota que
decide quando parar não está medindo nada — e era essa a versão que rodava
quando o Fábio digitava /revisar-servicos.

A cópia velha também tinha o escopo desatualizado: `seguranca` e `etica` como
"não editar", quando a skill já os havia trazido para o escopo editável, e
faltavam as verificações de âncora, de menu suspenso e de nomenclatura.

Duas fontes para o mesmo procedimento é o padrão dos espelhos de número: elas
divergem em silêncio, e a errada é descoberta pelo estrago. Agora há uma fonte
só, e este arquivo existe apenas para dar o atalho.
-->
