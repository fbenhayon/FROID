services:
  froid-edge:
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - froid-frontend
      - froid-backend
    restart: unless-stopped
  froid-backend:
    build: ./froid-server
    ports:
      - "8000:8000"
    environment:
      - GOOGLE_CLIENT_ID=109290160674-cbmk7j15patqk0679joi43lr2ndm5fal.apps.googleusercontent.com
      - GOOGLE_AUTH_DEV_FALLBACK=${GOOGLE_AUTH_DEV_FALLBACK:-true}
      - FROID_LOCAL_AUTH_EMAILS=${FROID_LOCAL_AUTH_EMAILS:-}
      - FROID_LOCAL_AUTH_PASSWORD=${FROID_LOCAL_AUTH_PASSWORD:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
      - OPENAI_TRANSCRIBE_MODEL=${OPENAI_TRANSCRIBE_MODEL:-gpt-4o-transcribe}
    restart: unless-stopped
  froid-frontend:
    build: ./froid-dashboard
    ports:
      - "5173:80"
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config:
